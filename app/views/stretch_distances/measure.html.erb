<div class="my-container" data-controller="stretch-analyzer">
  <h1>前屈測定</h1>

  <div class="mb-6 text-center">
    <p class="font-semibold mb-2">画像撮影時の注意点</p>
    <div class="inline-block text-left">
      <ul class="list-disc list-outside mb-2 ml-5">
        <li>真横から全身が映るように撮影してください。</li>
        <li>全身が認識しやすいよう背景はなるべく単色で撮影してください。</li>
      </ul>
    </div>
    <img src="<%= asset_path('zenkutsu_level_01.jpg') %>" alt="前屈画像の例" class="w-64 mx-auto rounded-lg mt-2">
  </div>

  <form data-action="submit->stretch-analyzer#submit" enctype="multipart/form-data">
    <div class="mb-4">
      <label for="image" class="my-label-title">立位前屈の画像アップロード</label>
      <input type="file" name="image" accept="image/*" data-stretch-analyzer-target="image" class="my-form-text">
      <div data-stretch-analyzer-target="imageError" class="text-sm text-red-600 mt-1"></div>
    </div>

    <div class="text-center">
      <button type="submit" class="my-orange-btn">解析する</button>
    </div>
  </form>

  <!-- 全体的なメッセージ -->
  <div data-stretch-analyzer-target="message" class="mt-4 text-center text-red-600"></div>

  <!-- MediaPipeの解析結果を描画（非表示） -->
  <canvas id="pose-canvas" class="hidden"></canvas>
</div>
